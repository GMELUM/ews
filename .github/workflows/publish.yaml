name: Publish package to GitHub Packages

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20.x'
          registry-url: 'https://npm.pkg.github.com'
          scope: '@gmelum' # Замените на свою организацию или имя пользователя 

      - name: Copy contents of 'dist' folder
        run: cp -r dist published-dist

      - name: Install dependencies
        run: npm ci

      - name: Publish to GitHub Packages
        run: npm publish published-dist
        env:
          NODE_AUTH_TOKEN: ${{ secrets.GITHUB_TOKEN }}